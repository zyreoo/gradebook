<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elevi - Catalog Online</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <div class="dashboard">
            <h1>Toți Elevii</h1>


            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>
            
            <% if (typeof success !== 'undefined' && success) { %>
                <div class="success-message">
                    <%= success %>
                </div>
            <% } %>
            
            <div class="navigation">
                <a href="/dashboard">← Înapoi la Panou</a>
            </div>

            <% if (students.length === 0) { %>
                <p>Niciun elev găsit.</p>
            <% } else { %>
                <div class="students-list">
                    <table>
                        <thead>
                            <tr>
                                <th>Nume</th>
                                <th>Email</th>
                                <th>Adaugă o notă</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% students.forEach(function(student) { %>
                                <tr>
                                    <td><%= student.name %></td>
                                    <td><%= student.email %></td>
                                    <td>
                                        <form action="/add-grade" method="POST" class="grade-form">

                                            <input type="hidden" name="studentId" value="<%= student.uid %>">
                                            
                                            <input 
                                            type="text" 
                                            name="subject" 
                                            placeholder="Materie (opțional)" 
                                            class="subject-input"
                                            >

                                            <select id="grade" name="grade" class="grade-select" required>
                                                <option value="">Selectează Nota</option>
                                                <option value="10">10 - Excelent</option>
                                                <option value="9">9 - Foarte Bine</option>
                                                <option value="8">8 - Bine</option>
                                                <option value="7">7 - Satisfăcător</option>
                                                <option value="6">6 - Suficient</option>
                                                <option value="5">5 - Insuficient</option>
                                                <option value="4">4 - Insuficient</option>
                                                <option value="3">3 - Insuficient</option>
                                                <option value="2">2 - Insuficient</option>
                                                <option value="1">1 - Insuficient</option>
                                            </select>
                                            <button type="submit" class="btn-add-grade">Adaugă</button>
                                        </form>
                                    </td>

                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } %>

            <div class="actions">
                <form action="/auth/logout" method="GET">
                    <button type="submit" class="btn-primary">Deconectare</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>