<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panou Profesor - Catalog Online</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1>Panou Profesor</h1>
            <div class="user-info">
                <p><strong>Bine ai venit, <%= user.name %>!</strong></p>
                <p>Email: <%= user.email %></p>
                <p>Rol: <%= user.role %></p>
            </div>
        </div>

        <div class="section">
            <h2>Clasele Mele (<%= assignedClasses.length %>)</h2>
            <% if (assignedClasses.length === 0) { %>
                <div class="empty-state">
                    <p>Nicio clasă alocată încă. Contactează administratorul școlii pentru a fi alocat la clase.</p>
                </div>
            <% } else { %>
                <div class="action-grid">
                    <% assignedClasses.forEach(function(classYear) { %>
                        <a href="/class/<%= classYear %>?view=teacher" class="action-card">
                            <div class="action-icon"><%= classYear.charAt(0).toUpperCase() %></div>
                            <h3>Clasa <%= classYear %></h3>
                            <p>Vizualizează elevii și gestionează notele</p>
                        </a>
                    <% }); %>
                </div>
            <% } %>
        </div>

        <% if (classmasterClasses && classmasterClasses.length > 0) { %>
            <div class="section classmaster-section">
                <h2>Clasele Mele Diriginte (<%= classmasterClasses.length %>)</h2>
                <p class="helper">Ca diriginte, poți vizualiza toate notele elevilor și motiva absențele.</p>
                <div class="action-grid">
                    <% classmasterClasses.forEach(function(className) { %>
                        <a href="/class/<%= className %>?view=classmaster" class="action-card classmaster-card">
                            <div class="action-icon">DIR</div>
                            <h3>Clasa <%= className %></h3>
                            <p>Vedere Diriginte - Toate notele & absențele</p>
                        </a>
                    <% }); %>
                </div>
            </div>
        <% } %>

        <div class="dashboard-footer">
            <form action="/auth/logout" method="GET">
                <button type="submit" class="btn-primary">Deconectare</button>
            </form>
        </div>
    </div>
</body>
</html>