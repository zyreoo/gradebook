<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School Admin Login - Online Gradebook</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <div class="login-card">
            <div class="admin-header-section">
                <div class="admin-badge">Administrator Portal</div>
                <h1>School Admin Login</h1>
                <p class="admin-subtitle">Manage your school's teachers and students</p>
            </div>
            
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>

            <% if (typeof success !== 'undefined' && success) { %>
                <div class="success-message">
                    <%= success %>
                </div>
            <% } %>

            <form action="/auth/admin/login" method="POST">
                <div class="form-group">
                    <label for="email">Administrator Email</label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        required 
                        autocomplete="email"
                        placeholder="admin@yourschool.edu"
                    >
                </div>

                <div class="form-group">
                    <label for="password">Password</label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required 
                        autocomplete="current-password"
                        placeholder="Enter your password"
                    >
                </div>

                <button type="submit" class="btn-primary">Sign In</button>
            </form>

            <div class="admin-info">
                <div class="admin-info-title">Administrator Access Only</div>
                <p>This portal is exclusively for school administrators. Teachers and students should use the standard login page to access their accounts.</p>
            </div>

            <div class="links-row">
                <a href="/auth/admin/register">Register New School</a>
                <a href="/auth/login" class="back-link">Back to Regular Login</a>
            </div>
        </div>
    </div>
</body>
</html>

